# Third-party notices

This file summarizes the licensing of third-party components used by POKE64. It is informational and is not legal advice. Definitive terms come from the exact upstream revisions and file-level notices used for a build.

## vice-libretro and VICE

POKE64 is designed to load a locally built `vice_x64sc_libretro` core.

- Core source: https://github.com/libretro/vice-libretro
- VICE source mirror: https://github.com/VICE-Team/svn-mirror
- Upstream license file: `COPYING`
- Main codebase license: GPL-2.0-or-later, subject to file-specific notices and bundled third-party components

The `vice-libretro` and VICE trees contain the GNU GPL version 2 license text, while source notices commonly permit redistribution under GPL version 2 or, at the recipient's option, a later version. A distributor must inspect the exact pinned source tree rather than rely only on this summary.

The VICE source tree, compiled dylib and firmware-verification reports are excluded from this repository. They are created locally during the build process. POKE64 applies a local GPL-side patch that restores the VICE MPS-803 interpreter and replaces the desktop graphics exporter with a sandbox-safe grayscale page spool used by the iOS frontend.

The POKE64 build script removes detected exact firmware payloads from the unsigned core and refuses to finish when expected categories cannot be verified. A distributor remains responsible for reviewing the generated artifact and satisfying all obligations for the selected upstream revision, including preserving notices and providing complete corresponding source in a GPL-compliant form.

The MIT license covering POKE64 application code does not replace or reduce the obligations attached to a distributed VICE-derived core.

## MEGA65 OpenROMs

POKE64 can download a pinned generic C64 system-ROM set from the MEGA65 OpenROMs project at runtime.

- Source: https://github.com/MEGA65/open-roms
- Primary project license: LGPL-3.0-or-later
- Additional notices: some files or included material have file-specific terms, including MIT-licensed BASIC material

POKE64 downloads only the pinned generic BASIC, KERNAL and character images selected in `FirmwareStore`. They are not committed to this repository. Release documentation should record the exact OpenROMs revision and preserve the applicable upstream notices.

OpenROMs does not provide the proprietary Commodore or JiffyDOS drive-ROM files accepted by POKE64's drive slots.

## Commodore and replacement firmware

POKE64 does not include original Commodore BASIC, KERNAL, character, drive or MPS-803 printer firmware, and does not include JiffyDOS. The application provides import slots for user-supplied files.

Copyright and licensing of imported firmware remain separate from POKE64, VICE and OpenROMs. Users and redistributors are responsible for ensuring that imported or redistributed firmware is lawfully obtained and used.

## libretro API header

`POKE64/LibretroHost/LibretroMinimal.h` contains the subset of declarations required by this frontend, derived from the libretro API header.

- Upstream header: https://github.com/libretro/RetroArch/blob/master/libretro-common/include/libretro.h
- License for `libretro.h`: MIT

The libretro API header contains its own MIT notice, separate from the GPL license used by the RetroArch application.

## XcodeGen

The Xcode project is generated by XcodeGen.

- Source: https://github.com/yonaskolb/XcodeGen
- License: MIT

## Apple frameworks

The project links Apple system frameworks including SwiftUI/UIKit, Metal/MetalKit, AVFoundation, AudioToolbox, Foundation, Game Controller and CryptoKit. Their use is governed by Apple's applicable SDK and developer agreements.

## Content

No game, program, disk image, tape image, cartridge image or commercial content is included in this repository or its release archives.

### POKE64 GPL VICE-core patches

For POKE64 0.7.5, the pinned `vice-libretro` revision is:

```text
c8c242db75a559246d6d51017e6dd4ecd75d6a9f
```

`Scripts/vice-patches/output-graphics-poke64.c` replaces a graphics
output backend inside the VICE-derived core.

`Scripts/vice-patches/rs232net-poke64-hayes.c` adds the POKE64 Virtual
Hayes modem layer used by the VICE RS-232 network backend.

Because these files are compiled and linked into the GPL VICE component,
they are intentionally licensed GPL-2.0-or-later and are not covered by
the repository's root MIT License.

The proprietary Commodore MPS-803 printer ROM is not included in the
repository, application bundle, generated core or release artifacts.
It must be supplied separately and legally by the user.
